(async()=>{const {data:{session:s}}=await sb.auth.getSession();if(s&&window.loadMasters)await loadMasters();})();const baseStart=window.startSession;window.startSession=async function(mode){if(mode==='choice'&&currentDeck?.question_mode==='shared_choices'){const u=[...new Set(currentCards.map(c=>c.answer))];if(u.length<4)return toast('同じ答えを共有する4択には4種類以上の答えが必要です');}return baseStart(mode)};